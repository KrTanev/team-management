# `npm run dev:mock` — the app runs against the in-browser mock API (src/mocks),
# no backend needed. The URL is only a label the mock intercepts.
VITE_API_URL="http://mock.api"
VITE_USE_MOCK_API="true"
